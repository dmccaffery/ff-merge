# dependencies and build cruft
node_modules/
coverage/
test-report.junit.xml
*.tsbuildinfo
.DS_Store

# local agent handoff (re-created each batch, never committed)
commit.sh

# local agent/editor settings
.claude/settings.local.json
.claude/plans
